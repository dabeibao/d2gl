# AGENTS.md

本文件用于指导 AI 代理（如 opencode / Claude / GPT 等）在本仓库工作。
本文件由 GLM-5.2-FP8 模型生成。

## 项目概述

- **D2GL**: Diablo 2 LoD Glide/DDraw 到 OpenGL 的 wrapper
- 平台: Windows, x86 (32-bit)
- 语言: C++20
- 构建系统: Visual Studio (`.sln` + `.vcxitems`), 编译器为 MSVC (cl.exe)
- 第三方依赖: stb_image, detours, glm, imgui, glslang, glew

## 构建与验证

仓库根目录的 `compile_commands.json` 仅用于 IDE/clangd 索引,**不是**构建系统。
没有 CMakeLists.txt。请在 Visual Studio 中打开 `d2gl.sln` 进行构建。

如果 PATH 中有 `clang++`,可对单文件做语法检查:

```powershell
clang++ -x c++ -std=c++20 -Wall -fms-compatibility-version=19.10 `
  -Wno-invalid-token-paste -Wno-unknown-pragmas -Wno-unused-value -m32 `
  -fsyntax-only -DUNICODE -D_UNICODE -DWIN32 -DGLEW_STATIC -DGLIDE3X_EXPORTS `
  -DD2GL_BUILD -I d2gl/src -I d2gl/vendor/include -I glide3x/src -I d2gl `
  path/to/file.cpp
```

无 lint / typecheck 脚本。改动后请人工 review diff。

## 代码约定

- 缩进: Tab (NOT spaces)
- 命名: `m_xxx` 成员变量, `g_xxx` 全局变量, `XXX_YYY` 常量/宏
- 头文件保护: `#pragma once`
- C++20 特性允许使用 (结构化绑定, `std::make_unique` 等)
- 字符编码: 源文件 UTF-8, 字符串处理使用 `wchar_t` / `L"..."` (Windows API 兼容)
- 注释: 必要时使用英文注释, 与现有代码风格一致

## 关键模块说明

### HD Text (`d2gl/src/modules/hd_text/`)

MSDF (Multi-channel Signed Distance Field) 字体渲染系统。

- `glyph_set.{h,cpp}`: 字形集管理。从 PNG atlas 加载,支持 lazy-load
  - `m_glyphes`: 已加载的字形 map (`wchar_t -> Glyph`)
  - `m_page_glyphs`: 每 page 的字形 CSV 解析结果,加载前的 metrics 缓存
  - `m_page_states`: page 加载状态 (NOT_LOADED / LOADING / LOADED)
  - **sentinel 约定**: `tex_id == 0xFFFF` 表示 placeholder/dummy glyph,`drawChar` 应跳过渲染
- `font.{h,cpp}`: 字体类,封装字形集 + 大小/颜色/对齐等
- `hd_text.{h,cpp}`: HD Text 模块入口,管理多字体 + 文本布局

### 颜色 LUT (`d2gl/src/d2/item_color_lut.{h,cpp}`)

3D LUT 用于物品染色,使用 DXT5 压缩以节省显存。

## AI 改写约定

**所有 AI 生成或大幅改写的代码,在 commit message 中必须注明模型名**:

格式示例:
```
<commit message>

AI-generated by <ModelName>
```

如: `AI-generated by GLM-5.2-FP8`, `AI-generated by Claude-3.5-Sonnet`, `AI-generated by GPT-4o` 等。

AI 提交时使用 `git commit --author="<ModelName> <ai@local>"` 设置作者信息。

AI 改写代码时请:

1. 保留原文件 header 注释 (copyright / license)
2. 使用与周围代码一致的缩进 (Tab) 和命名风格
3. 必要时加注释解释非显然的逻辑 (sentinel 值、异步加载时机等)
4. 修改 API 后更新相关头文件
5. 不要添加冗余的 `#include`,除非新代码确实需要
